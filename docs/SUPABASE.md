# Supabase (legacy)

Tideway migrated to **Neon Postgres** in August 2026.

See [NEON.md](./NEON.md) for the current setup.

SQL migration files remain under `supabase/migrations/` for history; they are applied to Neon via `npm run db:migrate`.
